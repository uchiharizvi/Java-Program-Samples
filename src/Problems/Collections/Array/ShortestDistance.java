package Problems.Collections.Array;

import java.util.Arrays;
import java.util.Comparator;
import java.util.List;
import java.util.stream.Collectors;

public class ShortestDistance {
    public static void main(String[] args) {
        /**From a given array of natural numbers, return the distance between the two closest numbers.*/
        int[] numbers = {10,9,54,6,76,53,45,67};
        //sort array
        //List<Integer> numberList = (List<Integer>) Arrays.stream(numbers).sorted().collect(Collectors.toList());
    }
}
